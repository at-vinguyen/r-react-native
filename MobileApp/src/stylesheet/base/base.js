import color from 'moneysaving/src/stylesheet/base/variable';
import { StyleSheet } from 'react-native';

export default styleBase = StyleSheet.create({
  h1: {
    fontSize: 48
  },
  textCenter: {
    textAlign: 'center'
  }
});
