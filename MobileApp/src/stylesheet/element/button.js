
import color from 'moneysaving/src/stylesheet/base/variable';
import { StyleSheet } from 'react-native';

export default styleButton = StyleSheet.create({
  btn: {
    backgroundColor: color.primary,
    borderColor: 'transparent',
    borderWidth: 0,
    borderRadius: 5
  },
  input: {
    borderColor: '#CCCCCC'
  }
});
